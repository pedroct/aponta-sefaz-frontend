name: aponta-staging-frontend

services:
  frontend:
    image: ghcr.io/pedroct/aponta-sefaz-frontend/fe-aponta:staging
    build: null
    container_name: fe-aponta-staging
    ports: []
    networks:
      - aponta-shared-network

networks:
  aponta-shared-network:
    external: true
    name: aponta-shared-network
