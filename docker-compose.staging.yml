# docker-compose.staging.yml
# Override para VPS staging
# Uso: docker compose -f docker-compose.yml -f docker-compose.staging.yml up -d --build
#
# Portas: nao expoe portas no host. O nginx-aponta (reverse proxy)
# acessa fe-aponta-staging:80 pela rede interna aponta-shared-network.

services:
  frontend:
    container_name: fe-aponta-staging
    ports: []

networks:
  aponta-network:
    external: true
    name: aponta-shared-network
